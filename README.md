
gob24/
├── go.mod
├── go.sum
├── main.go                # Точка входа: считывает конфиги, запускает сервер
├── internal/
│   ├── bitrix/            # 1. СЛОЙ ИНФРАСТРУКТУРЫ (Репозитории / Клиенты)
│   │   ├── client.go      # Инициализация b24gosdk
│   │   ├── product.go     # Низкоуровневые вызовы (add, list, update)
│   │   └── catalog.go     # Универсальные методы и работа с каталогами
│   │
│   ├── sync/              # 2. СЛОЙ СЕРВИСОВ (Бизнес-логика / Use Cases)
│   │   └── product_sync.go# Логика: сопоставление полей 1С, проверка статусов
│   │
│   └── transport/         # 3. СЛОЙ ТРАНСПОРТА (Входные точки данных)
│       └── http.go        # Приемник HTTP-запросов от 1С
